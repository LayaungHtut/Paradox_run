// The game is a client-rendered app: canvas, WebAudio and localStorage all live in the browser.
// API endpoints under /api still run on the server.
export const ssr = false;
export const prerender = false;
